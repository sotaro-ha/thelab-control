# テストガイド

## 動作確認の手順

### 1. 環境の準備

#### 必要なもの
- サーバーマシン（Node.js実行環境）
- ロボット（actuator.cpp実行済み）
- 同じWi-Fiネットワーク
- Webカメラ

#### ネットワーク確認

**サーバー側:**
```bash
# macOS/Linux
ifconfig | grep "inet "

# Windows
ipconfig
```

**ロボット側:**
- シリアルモニタでIPアドレスを確認
- Wi-Fi接続メッセージをチェック

### 2. ステップバイステップテスト

#### Step 1: サーバー起動テスト

```bash
# サーバーディレクトリに移動
cd server

# 依存関係インストール（初回のみ）
npm install

# サーバー起動
npm start
```

**期待される出力:**
```
🚀 Server running on http://localhost:3001
📡 Listening for QubiLink devices on port 12340
🔍 Discovery listener on port 12340
📡 Discovery request sent
```

#### Step 2: デバイス検出テスト

ロボットを起動した状態で、サーバーログを確認：

**成功時の出力:**
```
📡 Device discovered: actuator_01 at 192.168.1.100:12345
```

**デバイスリストの確認:**
```bash
curl http://localhost:3001/api/devices
```

**期待されるレスポンス:**
```json
{
  "devices": [
    {
      "id": "actuator_01",
      "ip": "192.168.1.100",
      "controlPort": 12345,
      "capabilities": {
        "control": true,
        "video_in": false,
        "video_out": false
      },
      "lastSeen": 1234567890
    }
  ]
}
```

#### Step 3: 手動コマンド送信テスト

デバイスが検出されたら、手動でサーボコマンドを送信：

```bash
# 90度に設定
curl -X POST http://localhost:3001/api/command/actuator_01 \
  -H "Content-Type: application/json" \
  -d '{"angle": 90}'

# 0度に設定
curl -X POST http://localhost:3001/api/command/actuator_01 \
  -H "Content-Type: application/json" \
  -d '{"angle": 0}'

# 180度に設定
curl -X POST http://localhost:3001/api/command/actuator_01 \
  -H "Content-Type: application/json" \
  -d '{"angle": 180}'
```

**サーバーログ確認:**
```
🤖 Sent to actuator_01: angle=90
🤖 Sent to actuator_01: angle=0
🤖 Sent to actuator_01: angle=180
```

**ロボット側確認:**
- サーボが実際に動くか確認
- シリアルモニタでコマンド受信を確認

#### Step 4: フロントエンド起動テスト

別のターミナルで：

```bash
# プロジェクトルートに戻る
cd ..

# 依存関係インストール（初回のみ）
npm install

# 開発サーバー起動
npm run dev
```

**期待される出力:**
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

ブラウザで `http://localhost:3000` にアクセス。

#### Step 5: WebSocket接続テスト

1. ブラウザで `http://localhost:3000/motion` にアクセス
2. カメラ権限を許可
3. ブラウザのコンソール（F12）を開く

**期待されるメッセージ:**
```
WebSocket connected
```

**サーバーログ確認:**
```
🔌 New WebSocket client connected
```

#### Step 6: ポーズトラッキングテスト

1. カメラの前に立つ
2. 右手を上下に動かす
3. サーボが手の動きに追従するか確認

**サーバーログ確認（リアルタイム）:**
```
🤖 Sent to actuator_01: angle=150
🤖 Sent to actuator_01: angle=120
🤖 Sent to actuator_01: angle=90
🤖 Sent to actuator_01: angle=60
```

### 3. トラブルシューティング用テストスクリプト

#### ディスカバリーリスナーテスト

```bash
cd server
node test-discovery.js
```

ロボットからのアナウンスメッセージが表示されるか確認：

```
📡 Received from 192.168.1.100:12340
{
  "type": "announce",
  "proto": "qubilink",
  "ver": 1,
  "device_id": "actuator_01",
  ...
}
```

#### 直接コントロールテスト

```bash
cd server
node test-control.js 192.168.1.100  # ロボットのIPアドレス
```

**期待される出力:**
```
🤖 Servo Control Test
Target: 192.168.1.100:12345
Sending test sequence...

✅ Sent to 192.168.1.100:12345 - angle: 90
✅ Sent to 192.168.1.100:12345 - angle: 0
✅ Sent to 192.168.1.100:12345 - angle: 180
✅ Sent to 192.168.1.100:12345 - angle: 90

✅ Test sequence completed
```

### 4. パフォーマンステスト

#### フレームレート確認

ブラウザコンソールで：

```javascript
let frameCount = 0
let lastTime = Date.now()

setInterval(() => {
  const now = Date.now()
  const fps = frameCount / ((now - lastTime) / 1000)
  console.log(`FPS: ${fps.toFixed(2)}`)
  frameCount = 0
  lastTime = now
}, 1000)

// ポーズデータが送信されるたびにカウント
// （websocket.jsのsendPoseData関数内に追加）
frameCount++
```

**推奨FPS:** 10-20 FPS

#### レイテンシー確認

```javascript
// Motion.jsxでタイムスタンプを送信
const timestamp = Date.now()

// サーバー側で受信時刻を記録
const latency = Date.now() - data.timestamp
console.log(`Latency: ${latency}ms`)
```

**推奨レイテンシー:** < 100ms

### 5. 統合テスト

#### 完全フローテスト

1. サーバー起動
2. ロボット起動・検出確認
3. ブラウザでホームページアクセス
4. 「説明を見る」をクリック→スライドショー表示確認
5. 「展示を体験してみよう」をクリック→`/motion`に遷移
6. カメラ起動確認
7. 右手を上げる→サーボが180度付近に動く
8. 右手を下げる→サーボが0度付近に動く
9. 「ホームに戻る」をクリック
10. 感想を入力して送信

**各ステップでの確認ポイント:**
- UIが正しく表示されるか
- 遷移がスムーズか
- サーボの動きが滑らかか
- エラーがコンソールに出ていないか

### 6. 負荷テスト

#### 複数デバイステスト

複数のロボットを同時に制御する場合：

```bash
# デバイスリストを確認
curl http://localhost:3001/api/devices

# 各デバイスに個別コマンド送信
curl -X POST http://localhost:3001/api/command/actuator_01 \
  -H "Content-Type: application/json" \
  -d '{"angle": 90}'

curl -X POST http://localhost:3001/api/command/actuator_02 \
  -H "Content-Type: application/json" \
  -d '{"angle": 45}'
```

#### 長時間運用テスト

- 1時間以上連続でポーズトラッキングを実行
- メモリリーク確認
- デバイス接続の安定性確認
- パフォーマンス劣化の有無

### 7. エラーハンドリングテスト

#### ネットワーク切断テスト

1. ポーズトラッキング中にロボットのWi-Fiを切断
2. サーバーログで「Device timeout」メッセージを確認
3. ロボットを再接続
4. 自動的に再検出されるか確認

#### カメラエラーテスト

1. カメラを接続せずに`/motion`にアクセス
2. エラーメッセージが表示されるか確認
3. 「戻る」ボタンが機能するか確認

#### WebSocket切断テスト

1. ポーズトラッキング中にサーバーを停止
2. フロントエンドで再接続を試みるか確認
3. サーバーを再起動
4. 自動的に再接続されるか確認

## チェックリスト

### 基本動作
- [ ] サーバーが起動する
- [ ] デバイスが自動検出される
- [ ] 手動コマンドでサーボが動く
- [ ] フロントエンドが起動する
- [ ] WebSocket接続が確立する

### ポーズトラッキング
- [ ] カメラが起動する
- [ ] ポーズランドマークが表示される
- [ ] 右手の動きでサーボが動く
- [ ] 動きが滑らかである
- [ ] レイテンシーが低い（< 100ms）

### UI/UX
- [ ] ホームページが正しく表示される
- [ ] スライドショーが動作する
- [ ] ページ遷移がスムーズ
- [ ] 感想送信が機能する
- [ ] エラーメッセージが適切に表示される

### ネットワーク
- [ ] 同じネットワーク内で通信できる
- [ ] ファイアウォールで遮断されない
- [ ] ブロードキャストが届く
- [ ] ユニキャストが届く

### エラーハンドリング
- [ ] デバイス切断時に適切に処理される
- [ ] カメラエラーが表示される
- [ ] WebSocket切断時に再接続を試みる
- [ ] タイムアウトが正しく動作する

## デバッグTips

### ログレベルの調整

`server.js`の先頭に追加：

```javascript
const DEBUG = true

function debugLog(...args) {
  if (DEBUG) {
    console.log('[DEBUG]', ...args)
  }
}
```

### パケットキャプチャ

```bash
# macOS/Linux
sudo tcpdump -i any -n udp port 12340 or udp port 12345

# Wireshark を使用
# フィルター: udp.port == 12340 || udp.port == 12345
```

### ブラウザDevTools

1. F12でDevToolsを開く
2. Consoleタブでエラー確認
3. Networkタブで WebSocket通信確認
4. Performanceタブでフレームレート確認

## 問題報告時の情報

問題が発生した場合、以下の情報を記録：

1. **環境情報**
   - OS（macOS/Windows/Linux）
   - Node.jsバージョン
   - ブラウザとバージョン
   - ネットワーク構成

2. **ログ**
   - サーバーコンソールの出力
   - ブラウザコンソールの出力
   - ロボットのシリアルモニタ出力

3. **再現手順**
   - 具体的な操作手順
   - 期待される動作
   - 実際の動作

4. **スクリーンショット/動画**
   - エラー画面
   - 動作の様子
